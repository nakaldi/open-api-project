<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.open_api_project.module.place.repository.PlaceMapper">

    <select id="findAllByMemberId" resultType="com.example.open_api_project.module.place.dto.PlaceDto">
        SELECT
        id,
        member_id,
        place_name,
        description,
        latitude,
        longitude,
        created_at
        FROM places
        WHERE member_id = #{memberId}
        ORDER BY id DESC
    </select>

    <insert id="save" parameterType="com.example.open_api_project.module.place.dto.PlaceDto">
        INSERT INTO places (
        member_id,
        place_name,
        description,
        latitude,
        longitude
        ) VALUES (
        #{memberId},
        #{placeName},
        #{description},
        #{latitude},
        #{longitude}
        )
    </insert>

</mapper>